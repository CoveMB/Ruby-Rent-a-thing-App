<p>Filter By Category</p>

<div class="list-group">
    <%  @categories.each do |cat| %>
      <input form="myForm" type="checkbox" name="category[<%= cat.id %>]" class="list-group-item" data-category='<%=cat.name.split.join("-") %>' id='<%= cat.id %>'>
      <%= cat.name %><br>
    <% end %>
</div>
